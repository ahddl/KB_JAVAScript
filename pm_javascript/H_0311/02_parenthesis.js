// 문자열에 포함된 괄호들의 유효성을 검사하는 함수를 작성하세요.

// 예시:
// checkBrackets("(()){}[]") => true
// checkBrackets("({)}") => false
// checkBrackets("((()") => false

// 조건:
// - 지원하는 괄호: (), {}, []
// - 괄호 쌍이 맞아야 함
// - 괄호 순서가 맞아야 함
// - 다른 문자가 있어도 괄호만 검사

// 사용자에게 확인받는 함수
const readline = require("readline");

console.log(readline);

//replace," "공백을 두면 함수
